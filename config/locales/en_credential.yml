---
en:
  activerecord:
    models:
      credential:
        one: "credential"
        few: "credentials"
        many: "credentials"
        other: "credentials"
    attributes:
      credential:
        login: "Login"
        password: "Password"
        remember_me: "Remember me"
    errors:
      models:
        credential:
          header_message:
            one: "User login failed because of %{count} error"
            few: "User login failed because of %{count} errors"
            many: "User login failed because of %{count} errors"
            other: "User login failed because of %{count} errors"
          invalid_username_or_password: "Invalid username or password"
          invalid_user_status: "Current user's status is not active"
    hints:
      models:
        credential:
          attributes:
            password: "(case sensitive)"
