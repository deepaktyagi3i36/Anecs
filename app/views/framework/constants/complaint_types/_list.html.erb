<%= form_tag( {},{:id=>"list", :name=>"list"}) do %>
  <%= render :partial => "buttons" %>
  <table class="list-table">
    <col class="checkbox"/><col span=2 class="coldata"/>
    <col class="coldate"/><col class="actions"/>
    <tr>
      <th> <%= checkable_field_header %></th>
      <th> <%= sortable_field_header t("activeview.columns.complaint_type.name"), "complaint_types.name" %> </th>
      <th> <%= sortable_field_header t("activeview.columns.complaint_type.code"), "complaint_types.code" %> </th>
      <th> <%= sortable_field_header t("activeview.columns.complaint_type.created_at"), "complaint_types.created_at" %></th>
      <th> <%= t("activeview.columns.complaint_type.actions") %> </th>
    </tr>
    <% @complaint_types.each do |complaint_type| %>
      <tr>
        <td><%= check_box_tag('ids[]', complaint_type.id, false) %></td>
        <td><%= link_to complaint_type.name, :action => "edit", :id =>complaint_type %></td>
        <td><%= link_to complaint_type.code, :action => "edit", :id =>complaint_type %></td>
        <td><%= localize(complaint_type.created_at, :format => :long) %></td>
        <td>
          <%=link_to(image_tag("buttons/update_object.png", {:alt => "Update", :title => "Update"}), :action => "edit", :id => complaint_type) %>
          <%=link_to(image_tag("buttons/delete_object.png", {:alt => "Delete", :title => "Delete"}), :action => "destroy", :id => complaint_type) %>
        </td>
      </tr>
    <% end %>
  </table>
  <%= render :partial => "buttons" %>
<%end%>
