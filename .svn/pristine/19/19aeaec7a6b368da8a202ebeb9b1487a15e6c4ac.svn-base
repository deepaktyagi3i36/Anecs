echo off
rem examples of usage
rem
rem test:functionals - Run all functional tests.
rem test:units - Run all unit tests.
rem test:integration - Run all integration tests.
rem test:plugins - Run all test in ./vendor/plugins/**/test.
rem test:recent - Run tests for models and controllers that have been modified in the last 10 minutes:
rem test:uncommitted - For projects in Subversion, run tests for models and controllers changes since last commit

Rem set rails enviroment
set RAILS_ENV=development
rem set RAILS_ENV=production
rem set RAILS_ENV=test

echo on
cd ../
rake routes
cd ./cmd/
