echo off
rem examples of usage
rem

Rem set rails enviroment
rem set RAILS_ENV=development
rem set RAILS_ENV=production
rem set RAILS_ENV=test

echo on
cd ../
cap deploy
rem cap deploy:setup

rem cap production deploy
rem cap production deploy:setup

rem cap deploy:cold
rem cap deploy:pending
rem cap deploy:migrate
rem cap deploy:rollback
rem cap deploy:cleaup
rem cap deploy:setup
rem cap deploy:check
rem cap deploy:update
rem cap deploy:start
rem cap deploy:restart
cd ./cmd/
